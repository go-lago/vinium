# TODOS — Vinium
Generated by /autoplan | 2026-05-14

## P0: Fix Before Phase 3 (critical bugs + security)

### Security (must fix before any public deploy)
- [x] Fix OAuth redirect: access token leaks into URL → use one-time code exchange pattern
- [x] Fix refresh cookie: `Secure: false` hardcoded → read from env var `COOKIE_SECURE`, default true
- [x] Fix JWT_SECRET: empty string accepted → fail startup if len < 32 chars
- [x] Fix refresh token rotation: wrap read-delete-create in a DB transaction (race condition)
- [x] Add http.MaxBytesReader(1MB) before JSON decode in all handlers
- [ ] Add rate limiting on /auth/login and /auth/register (per-IP, sliding window)
- [x] Fix userID(r): panic-proof — return (uuid.UUID, bool), handle false case with 401

### Data integrity bugs
- [x] Fix autosave race: `scheduleSave` reads `contentRef.current` AT FIRE TIME, not at schedule time
- [x] Fix autosave: add `titleRef` mirroring `contentRef` (handleTitleChange updates ref synchronously)
- [x] Fix autosave: add `saveStatus = 'error'` state when save fails; show persistent retry UI
- [x] Fix autosave: clear timers on component unmount (useEffect cleanup)
- [x] Fix note delete: check `RowsAffected == 0`, return 404 sentinel (currently returns 204 for missing)
- [x] Replace `window.confirm()` in delete handler with inline confirm UI
- [x] Fix NotesPage: add .catch() to notesApi.list() call; show error state (not empty state)

## P1: Schema Changes (do before any data accumulates)

- [ ] Add `content_plain text` column to notes (extracted plaintext for search + AI)
- [ ] Add `content_version int default 1` column (migration safety)
- [ ] Add `type text default 'note'` column (values: note | meeting | voice | digest)
- [ ] Add `tags text[]` column
- [ ] Add `metadata jsonb` column (AI outputs, summaries, embedding refs)
- [ ] Add `deleted_at` timestamp column for soft delete (use gorm.DeletedAt)
- [ ] Fix FindByUserID: select summary fields only (id, user_id, title, type, tags, is_pinned, created_at, updated_at) — NOT content
- [ ] Add pagination to note list (page/per_page params, server-side max 100)
- [ ] Add FK constraint notes(user_id) → users(id) ON DELETE CASCADE

## P2: Architecture (before Phase 3)

- [ ] Add note.Service layer (handler → service → repo, not handler → repo directly)
- [ ] Add note.Service.ExtractPlainText(lexicalJSON string) string method
- [ ] Add note.Service.SearchNotes(userID, query string) for full-text search
- [ ] Add Lexical version pinning: store `lexical_version` in metadata, check on load

## P3: UX (complete Phase 2 unchecked items)

- [ ] Add text preview to note list cards (first 100 chars of content_plain)
- [ ] Add search bar to /notes page (backend full-text search via content_plain tsvector)
- [ ] Add pinned notes to top of list with visual separator
- [ ] Add Command Palette (⌘K) for quick note creation and search
- [ ] Redirect / (dashboard) to /notes — dashboard is a dead end until Phase 5
- [ ] Handle note creation loading state (disable "Новая заметка" while in-flight)
- [ ] Create note on first save, not on button click (avoid orphaned empty notes)

## P4: Tests (add alongside P0 fixes)

- [ ] Backend: concurrent refresh rotation test (race condition)
- [ ] Backend: cross-user note isolation test (user A cannot access user B's notes)
- [ ] Backend: Delete non-existent note → 404
- [ ] Backend: note list returns only current user's notes
- [ ] Frontend: autosave pin race test (pin before save fires)
- [ ] Frontend: save error shows error status (not silent idle)
- [ ] Frontend: no timer fire after unmount

## DEFERRED (Not in scope — confirmed by autoplan)

- ❌ Phase 4: News digest — cut entirely (use Readwise/Feedly)
- ❌ Contexts/workspaces system — Phase 6+ at earliest
- ❌ Tasks as entity — Phase 5 at earliest
- ❌ Events, Reminders, Files, Contacts — Phase 6+
- ❌ Graph/Relations model — Phase 6+
- ❌ Graph View visualization — v3.0+ (optional in PROJECT_DESCRIPTION)
- ❌ Collections/Views/Queries — Phase 6+
- ❌ Offline-first architecture — needs explicit design, not a Phase 2 add-on

## P5: UI Redesign (vinium-workspace-3.html reference)

The design file `vinium-workspace-3.html` defines the target visual system. Implement after P0+P1 are fixed.

### Design tokens (from vinium-workspace-3.html)
- Dark bg: #0d1117 | Surface: #161b22 | Accent: #2B73C4
- Fonts: Inter Tight (headings), Inter (body), JetBrains Mono (meta/code)
- Both dark and light themes (CSS tokens)

### Shell redesign
- [ ] Replace current wide text sidebar with narrow icon-only sidebar (48px, `--sidebar-w`)
- [ ] Add context tabs row in workspace header (Personal / Work / Business dots)
- [ ] Move search bar inline into workspace header (with ⌘K shortcut hint)
- [ ] Replace full top header with minimal status bar at bottom (word count, AI status dot)
- [ ] Add context switcher overlay (command palette-like, shown on context-dot click)

### Notes view redesign
- [ ] Replace border-box card list with feed-style rows (40px height, icon + title + meta)
- [ ] Add hover-reveal action buttons (pin, delete, AI actions) on note rows
- [ ] Add section headers with divider lines ("PINNED", "RECENT")
- [ ] Note row shows: type icon + title + tag chip + priority dot + updated time

### Editor redesign
- [ ] Replace full-width editor with split layout: ed-main (flex:1) + ed-sidebar (216px)
- [ ] Add breadcrumb navigation in editor header (Workspace > Note title)
- [ ] Add doc-meta-row below title (date, type tag, AI pill buttons)
- [ ] Add right sidebar panel: AI suggestions panel, note links, metadata
- [ ] Add AI pill inline buttons (summarize, expand, rephrase) as floating action

### Typography update
- [ ] Add Inter Tight + JetBrains Mono to index.html (Google Fonts)
- [ ] Update Tailwind config or CSS variables for the design token palette
- [ ] Apply --font-d (Inter Tight) to all headings and UI labels
- [ ] Apply --font-m (JetBrains Mono) to all metadata, timestamps, code

## REVISED PHASE SEQUENCE

Phase 3a (next, ~1-2 weeks): Fix ALL P0 bugs + P1 schema changes
Phase 3b (~1 week): UI Redesign (vinium-workspace-3.html → production components)
Phase 3c (~2-3 weeks): Add note.Service + AI text features (summarize, rephrase, expand) + Full-text search + MCP server (basic, read notes)
Phase 4 (~2 weeks): Command Palette (⌘K) + Note types (meeting, voice) + Voice → Note
Phase 5 (~3 weeks): Semantic search (embeddings) + AI Feed (right sidebar) + Chat with personal context + Weekly review
Phase 6: Tasks entity + Integrations (Calendar, GitHub, Telegram) + Shared workspace
